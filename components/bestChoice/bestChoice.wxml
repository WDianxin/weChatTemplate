<!--components/bestChoice/bestChoice.wxml智家优选-->
<view class="bestChoiceBox">
  <view wx:for="{{bastList}}" data-item="item" class="li" bindtap="toList" data-id="{{item.id}}" data-wareid="{{item.wareId}}" data-source="{{item.source}}" data-type="智家优选">
    <image src="{{item.wareIcon}}"></image>
    <text>{{item.wareName}}</text>
  </view>
</view>